# Codex Instructions

## Repos Folder

When a task refers to the repos folder, repositories folder, or asks for work to be done across repos by default, use the `REPOS_FOLDER` path defined in the local `.env` file.

Ignore repositories located under any `.archived` folder when scanning or performing batch work across repositories.
